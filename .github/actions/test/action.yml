name: "Test"
description: "Execute test"
inputs:
  directory:
    test-input: "Test input"
    required: true
    default: "haha"

runs:
  using: "composite"
  steps:
    - name: Set terraform version
      run: echo "${{ inputs.test-input }}"
      shell: bash
