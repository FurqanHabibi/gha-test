name: "Test"
description: "Execute test"

runs:
  using: "composite"
  steps:
    - name: Set terraform version
      run: |
        echo "${GITHUB_REF_NAME}"
        echo "${{ secrets.TEST }}"
      shell: bash
