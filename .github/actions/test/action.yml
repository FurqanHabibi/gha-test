name: "Test"
description: "Execute test"
inputs:
  test-input:
    description : 'tes aja'
    default: 'haha'

runs:
  using: "composite"
  steps:
    - name: Set terraform version
      run: echo "${{ inputs.test-input }}"
      shell: bash
