name: GitHub Actions Demo

on: 
  push:
    branches:
      - dev

jobs:
  build:
    name: Calver release
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include: [
          {dir: './switch-role-dev/tf', account: '164201395711'},
          {dir: './tf/hde-dev-cprd', account: '164201395711'}
        ]
    steps:
      - run: |
          echo "${{ matrix.dir }}" "${{ matrix.account }}"
