name: GitHub Actions Demo

on: push

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - uses: RaphaelNeumann/json-to-env-action@v1
        with:
          json_envs: '{"HOLA": "hello"}'
      - run: echo $HOLA
