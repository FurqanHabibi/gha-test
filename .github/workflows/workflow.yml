name: GitHub Actions Demo

on: push

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - run: exit 1
  job2:
    if: always()
    runs-on: ubuntu-latest
    needs: job1
    steps:
      - run: echo "This should not run"
